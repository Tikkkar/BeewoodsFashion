// ===== HƯỚNG DẪN SỬA LỖI =====
// Mở file: src/lib/api/admin.js
// 
// 1. Tìm dòng 96 (trong hàm createProduct):
//    await supabase.from("product_sizes").insert(sizePayload);
//
//    Thay thế bằng 2 dòng sau:
//    const { error: sizeError } = await supabase.from("product_sizes").insert(sizePayload);
//    if (sizeError) throw sizeError;
//
// 2. Tìm dòng 140 (trong hàm updateProduct):
//    await supabase.from("product_sizes").insert(sizePayload);
//
//    Thay thế bằng 2 dòng sau:
//    const { error: sizeError } = await supabase.from("product_sizes").insert(sizePayload);
//    if (sizeError) throw sizeError;
//
// Sau khi sửa xong, lưu file lại.
